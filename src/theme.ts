import { createTheme } from "@mui/material/styles";

// Create a theme instance.
export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#1a237e",
    },
    secondary: {
      main: "#ff6f00",
    },
  },
});
